# ── 原始数据：不进 git，只留在本机 ──────────────────────────
# 原始采集含真实 UP 的 mid/昵称、评论原文、无盐 uid 哈希 —— 见 README「隐私」。
# 发布版只带派生指标（data/videos.jsonl 匿名化、data/comments.jsonl 只有统计量）。
data/raw-local/

# ── Python ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── 编辑器 / 系统 ───────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
