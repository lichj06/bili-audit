# 发布文案

> **仓库**：https://github.com/lichj06/bili-audit
> **一句话**：正常视频之间的互动比差异有 **9.6–72.2 倍** —— 所以「用固定阈值判断刷量」在方法上就是错的。

---

## ⚠️ 发布前必读

**这份东西的立场是「分析数据形态」，不是「抓刷量」。** 发布时必须带上这四条，否则会被误读：

```
① 样本来自热门榜 —— 刷量视频通常上不了热榜，本项目建立的是【正常基线】不是抓取案例
② 全篇不做判定 —— 只报告指标与偏离度，不对任何视频或账号定性
③ 不做获取侧 —— 不提供任何刷量/水军相关的方法
④ 样本量很小 —— n=18（45 个视频里只有 18 个取到评论），所有比例类指标都别当结论
```

**另外三条硬边界（写死，不留解释空间）：**

```
· 不接受单账号委托 —— 单个视频没有分布，没有基线可依据
· 不出具单视频报告 —— 一份"某视频异常"的报告和指控没有区别
· 不接收/不转发"疑似刷量案例" —— 那会把基线项目变成打标工具
```

---

## ① 知乎 · 主推

**标题建议**：
- 《正常视频的互动比能差 72 倍，所以"刷量检测"大多是玄学》
- 《我爬了 45 个 B 站热门视频，想说一件关于"刷量检测"的事》

**正文**：

```
起因是想搞清楚一件事：B 站上那些"刷量检测"的说法，靠不靠谱。

于是用公开接口爬了 45 个热门视频的互动数据（播放/点赞/投币/收藏/评论）。
其中只有 18 个取到了评论，去重后 353 条唯一文本。结论比我预想的有意思：

【一】正常视频之间的互动比，差异有 9.6–72.2 倍

  点赞/播放   中位 4.72%   区间 1.46% – 14.03%    极差 9.6×
  投币/点赞   中位 26.53%  区间 1.06% – 76.35%    极差 72.2×
  收藏/点赞   中位 19.08%  区间 5.42% – 110.85%   极差 20.4×
  评论/播放   中位 0.34%   区间 0.12% – 1.43%     极差 11.8×
  （以上均为 n=18 的实测值，生成命令 python3 analysis/analyze.py）

  这意味着什么？

    ❌ "点赞率 > 10% 就是刷的"  →  这套样本里就有 1 个视频点赞率 14.03%
    ✅ 必须和同期长、同类型的分布比，看偏离度

  大部分"刷量检测"给出的结论经不起反驳，就是因为阈值没有分布基础。

【二】我一开始得出了三个"评论模板化"的视频，然后发现那是采集器自己的 bug

  B 站评论接口的 pn 参数不生效：pn=1/2/3 返回的是同一批评论。
  我原来的采集器老老实实翻了三页，于是 1086 行里有大半是同一页抄了三遍 ——
  重复率当然高，"模板化"当然触发。

  按 (视频, 文本) 去重后：353 条唯一文本，前 8 字重复率中位数 0，最大值 5.3%。
  三个"模板化"视频全部消失。这提醒我：指标异常时，先怀疑自己的管道。

【三】顺便发现了个有意思的指标：「问号指数」

  我统计了评论里含 ?/？ 的比例，前两名是两条锐评类视频（21.1% / 20.0%）。
  问号可能是观众被震到的标记。

  但必须说清楚：n=18、每视频约 20 条评论，分不出"内容形态导致问号"
  还是"这两条恰好如此"。而"？？？"（3 个以上连续问号）在本批 353 条唯一评论里
  一条都没有 —— 所以那个更酷的"三重问号率"这次等于没测出来。

【边界】

  · 样本来自热门榜，本来就抓不到刷量视频 —— 我建的是【正常基线】
  · n=18，样本很小；分区字段 tname 全为空，所以"和同分区比"我也做不到
  · 弹幕接口需要登录，本轮没拿到 —— 那本是问号的最佳数据源
  · 全篇不做判定，只报告偏离度
  · 不接受单账号委托、不出具单视频报告

  数据与代码全开源（含接口踩坑记录）：
  https://github.com/lichj06/bili-audit
```

---

## ② V2EX · 技术向

**标题**：爬了 45 个 B 站热门视频建互动比基线，顺手记录了接口的几个坑

**正文**：

```
做这个的起因是想验证"刷量检测"到底靠不靠谱。

采集（全部公开接口，无需登录）：
  x/web-interface/popular    热门榜，批量取视频
  x/web-interface/view       视频 stat（播放/点赞/投币/收藏/分享/评论/弹幕）
  x/v2/reply/main            评论

【几个实测出来的坑，可能对做同类事情的人有用】

① x/v2/reply 只返回 3 条
   换成 x/v2/reply/main?type=1&oid=&mode=3&ps=20&pn=1 才给满 20 条

② 评论分页是假的（这个坑最贵）
   pn=1/2/3 返回同一批 rpid（实测 314719551921 / 318314835872）。
   我第一版老老实实翻 3 页，结果 1086 行里大半是同一页抄三遍，
   下游"评论重复率"指标全部失真 —— 指标异常时先怀疑自己的管道。
   现在采集器只取 1 页，并在发现重复页时提前停止。

③ 别丢返回码
   第一版 except 里直接 break，把接口的 code/message 扔了，
   于是我在报告里写下"约 60–80 次请求后开始限流"—— 这句话没有任何日志支撑。
   现在每次请求都写 fetch_log.jsonl（含 code）。真相是：45 个视频里 27 个
   的评论接口返回 0 条，到底为什么，第一版已经查不出来了。

④ 弹幕接口现在拿不到
   新端点 x/v2/dm/web/seg.so 返回的是配置信息不是弹幕列表
   旧端点 comment.bilibili.com/<aid>.xml 用 raw-deflate 解压后
   只剩 <i> 头部，没有 <d> 元素 → 需要登录 + 签名

【数据结论】

  正常视频的互动比差异是 9.6–72.2 倍（n=18）：
    点赞/播放 1.46%–14.03% · 投币/点赞 1.06%–76.35% · 收藏/点赞 5.42%–110.85%
  → 固定阈值的刷量检测方法上就是错的，必须用相对同群分布的稳健偏离度
  → 我用有符号稳健 z =(x−median)/(1.4826×MAD)，|z|≥3 作判据，方向性指标取单侧

  另一个发现：文本重复度无法区分「观众跟风刷梗」与「水军灌入」，
  特征相同、性质相反 → 单一指标不足以判定

【实现细节】
  · 图表没装 matplotlib，手写 SVG 生成，零依赖
  · 图表的四分位数与报告表共用同一个 percentile() 函数，脚本结尾会断言一致
    （第一版两处算法不同，同一批数据的"中位数"差了 2 个百分点）
  · 弹幕 protobuf 我写了手解器（后来发现用不上，留在代码里了）
  · 发布版数据做了匿名化：真标题/UP 名/评论文本/uid 只留在本机 raw-local/
    （原来存的 sha1(mid)[:12] 是无盐的，一次公开 API 就能反查回 mid，已不再保存）

https://github.com/lichj06/bili-audit

声明：样本来自热门榜，本来就抓不到刷量视频，我建的是正常基线；全篇不做判定；
不接受单账号委托，不出具单视频报告。
```

---

## ③ B 站专栏 / 动态（如果发在 B 站）

**标题**：正常视频的互动比能差 72 倍，所以"刷量检测"大多是玄学

**正文**：

```
爬了 45 个热门视频的互动数据，建了个基线（其中 18 个取到评论）。

最意外的一条：正常视频之间的互动比差异有 9.6–72.2 倍。
  投币率正常区间 1.06%–76.35%，收藏率 5.42%–110.85%（收藏能超过点赞）

所以"点赞率超过 X% 就是刷的"这类说法，方法上就是错的。

另一个发现：文本重复度区分不了「观众跟风刷梗」和「水军灌入」——
而且我第一次跑出来的三个"模板化"视频，最后查出来是我自己采集器
把同一页评论抄了三遍导致的，不是评论区真的在刷。

顺便做了个「问号指数」（评论里含 ?/？ 的比例）：
  排最前的是锐评类内容。但 n 只有 18，别当结论看。
  至于"？？？"（三个以上连续问号），这批 353 条唯一评论里一条都没有。

数据和代码开源：github.com/lichj06/bili-audit

（只做分析，不做判定；样本来自热门榜，本身就是正常基线；
  不接受单账号委托，不出具单视频报告）
```

---

## ④ 一句话版

```
爬了 45 个 B 站热门视频建互动比基线（18 个取到评论）：
正常视频的点赞率差 9.6 倍、投币率差 72.2 倍。
→ 「用固定阈值判刷量」方法上就是错的。
→ 另一个发现：文本重复度区分不了「跟风刷梗」与「水军灌入」。
数据与代码开源：https://github.com/lichj06/bili-audit
```

---

## ⑤ 发布顺序

```
第 1 天   知乎（数据分析向，受众最对口）
第 1 天   V2EX（技术向，接口踩坑对开发者有用）
第 2 天   B 站专栏/动态（如果要在这边长账号）
随时      短文案
```

## ⑥ 有人质疑"这个指标怎么算的"

这是最欢迎的一类反馈。回答方式固定：

```
① 给命令：python3 analysis/analyze.py（会打印每个数字的来源）
② 给样本量：n=18，每视频 18–21 条唯一评论 —— 比例类指标置信区间很宽
③ 说明该指标在本批数据里是否可达（报告里有"可达性"表）
④ 如果对方能提供时间序列（连续采几天），那就能做增长曲线检测
   —— 那是本项目目前做不到的部分
```

**注意：本项目不接受"我怀疑某个视频刷了，你帮我看看"这类委托。**
单视频没有分布，就没有基线可依据；出一份单视频报告，等于出指控。
可以做的只有：把指标算出来放进基线看位置，并明确说明这不构成任何定性。

---

## ⑦ 为什么"问号指数"值得单独讲（内容侧方法论）

`？？？` 是**预期违背**的直接标记 —— 观众被内容震到、看不懂、或发现反转时才会打出来。
按这个假设，内容里有四种结构会稳定地制造落差：

```
① 反转结构    先给一个预期，再当场推翻它        → "？？？"
② 信息缺口    抛出一个你答不上来的具体问题      → "？？？"
③ 尺度错位    熟悉的东西出现在完全不该在的地方  → "？？？"
④ 身份冲突    说话人与话题之间的错配            → "？？？"
```

> ⚠️ **这是假设，不是本批数据的结论。** 本批 n=18、每视频约 20 条评论，
> 且"3 个以上连续问号"在本批数据里出现 0 次 —— 上面四条**没有被本批数据检验过**。
> 写在这里是因为它是这个指标的设计动机，不是因为它被验证了。
